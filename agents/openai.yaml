interface:
  display_name: "Battery Evidence & Mechanism Audit"
  short_description: "Source checks, adaptive depth, and independent research agents"
  default_prompt: "Use $expert-electrochemistry-public to investigate this battery-materials claim, verify decisive sources, use independent subagents where useful, and give a bounded conclusion with the next discriminating control."
policy:
  allow_implicit_invocation: true
